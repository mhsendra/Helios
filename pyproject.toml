[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "helios"
version = "1.0.0"
description = "Herramienta de análisis, simulación y optimización energética para viviendas con instalaciones fotovoltaicas."
requires-python = ">=3.11"